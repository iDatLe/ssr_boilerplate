import { hot } from 'react-hot-loader';
import React from 'react';
import './app.css';

const App = () => {
  return (
    <div>
      Welcome!!!!!
    </div>
  )
}

export default hot(module)(App);